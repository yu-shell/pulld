node_modules/
.DS_Store
*.log
.env
.env.*
!.env.example
.wrangler/
dist/

# Build output (generated by `npm run registry:build`). Hand-authored static pages in public/
# (e.g. account.html) stay tracked; only the generated files are ignored.
public/index.html
public/r/
public/llms.txt

# Premium sources are kept private; only the free core is published.
/pro/
functions/_pro-blocks.js

# Internal working notes (kept local, not published)
criteria.md
roadmap.md
ROUTINE.md
QUALITY-SWEEP.md

# Operational state (kept local)
/data/
